{
  "name": "pj-air",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run start:dev --workspace=backend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "seed": "ts-node scripts/seed-admin.ts",
    "check-images": "node check-db-images.js",
    "test:config": "node scripts/test-startup-config.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "sqlite3": "^5.1.7",
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
